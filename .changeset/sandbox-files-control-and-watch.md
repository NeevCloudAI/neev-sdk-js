---
"@neevcloud/sdk": minor
---

Add the remaining sandbox filesystem operations to `sandbox.files`: `stat`, `exists`, `mkdir`, `move`, `remove`, and a streaming `watch` that yields `WatchEvent`s as the workspace changes.
